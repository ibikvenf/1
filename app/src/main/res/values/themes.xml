<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.AegisAV" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryContainer">@color/primary_container</item>
        <item name="colorOnPrimaryContainer">@color/on_primary_container</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor">@color/toolbar_bg</item>
    </style>

    <!-- 分享查杀：半透明浮窗式 Activity -->
    <style name="Theme.AegisAV.Translucent" parent="Theme.AegisAV">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="Toolbar.Aegis" parent="Widget.Material3.Toolbar">
        <item name="android:background">@color/toolbar_bg</item>
        <item name="titleTextColor">@color/toolbar_text</item>
        <item name="colorControlNormal">@color/toolbar_text</item>
    </style>

    <style name="Card.Aegis" parent="Widget.Material3.CardView.Elevated">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">3dp</item>
    </style>

    <style name="Button.Aegis.Text" parent="Widget.Material3.Button.TextButton">
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">40dp</item>
    </style>
</resources>
